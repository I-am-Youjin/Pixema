import React from "react";
import { PropsWithChildren } from "react";
import { StyledH1, StyledH2, StyledH3 } from "./stryles";

interface ITitleProps extends PropsWithChildren {
  variant: "";
}
const Title = () => {
  return <div></div>;
};

export default Title;
